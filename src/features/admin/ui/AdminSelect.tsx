"use client";

/**
 * Admin select — same animated dropdown panel as checkout/storefront.
 * Prefer this over native `<select>` / `.dropdown-native`.
 */
export {
  CheckoutSelect as AdminSelect,
  type CheckoutSelectOption as AdminSelectOption,
} from "@/features/checkout/ui/CheckoutSelect";
