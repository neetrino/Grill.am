/** Deterministic E2E operator for admin UX checks (mock mode only). */
export const E2E_OPERATOR_EMAIL = "e2e-operator@grill.local";
/** Meets project password rules; never use in production. */
export const E2E_OPERATOR_PASSWORD = "E2eOp!Pass2026";
