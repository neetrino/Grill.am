import localFont from "next/font/local";

/** Figma footer watermark typeface: Mirage Bold Free (Alpaprana Studio). */
export const mirageBoldFree = localFont({
  src: "../../assets/fonts/MirageBoldFree.otf",
  variable: "--font-mirage-bold",
  display: "swap",
  weight: "400",
});
